package com.grigoriyalexeev.statistician.core.impl;

import org.junit.Ignore;
import org.junit.Test;

public class InBlogsWordUsageStatisticianTest {
    @Ignore
    @Test
    public void measure() throws Exception {
//        WordFinder wordFinder = mock(WordFinder.class);
//        when(wordFinder.find(anyString())).thenReturn(Collections.EMPTY_LIST);
//        WordUsageStatisticsAssembler statisticsAssembler = mock(WordUsageStatisticsAssembler.class);
//        when(statisticsAssembler.assemble(anyList())).thenReturn(Collections.EMPTY_MAP);
//        Representer representer = mock(Representer.class);
//        when(representer.format(anyObject())).thenReturn("");
//        InBlogsWordUsageStatistician statistician = new InBlogsWordUsageStatistician(wordFinder, statisticsAssembler
////                , representer
//        );
//        List<String> words = Arrays.asList("test", "test");
//
////        String statistics = statistician.measure(words);
//
//        verify(wordFinder, times(words.size())).find(anyString());
//        verify(statisticsAssembler, times(words.size())).assemble(anyList());
//        verify(representer, times(1)).format(anyObject());
    }
}